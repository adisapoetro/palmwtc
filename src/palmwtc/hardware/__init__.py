"""palmwtc.hardware — GPU/MPS-aware optional accelerators.

Phase 2 ports from ``flux_chamber/src/gpu_utils.py``. Imports are lazy so the
core install (no ``[gpu]`` or ``[ml]``) does not require torch/sklearn.
"""
