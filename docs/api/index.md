# API Reference

Auto-generated from docstrings via `sphinx-autoapi`. The API surface is
populated as modules are ported in Phase 2.

```{toctree}
:maxdepth: 2

palmwtc/index
```

(If you see "no module found" warnings during the docs build, that's expected
in Phase 1 — modules are stubs. They get real content in Phase 2.)
